package com.atmospherebbdo.collections {	import com.atmospherebbdo.collections.AbstractCollection;	import com.atmospherebbdo.dbc.precondition;	import com.atmospherebbdo.errors.AbstractMethodCallError;	import com.atmospherebbdo.util.instantiatedAs;		/**	 * @author markhawley	 * 	 * Base array-based collection iterator.	 */	internal class AbstractArrayIterator extends AbstractIterator	{		protected var impl:Array;		protected var index:int = -1;				/**		 * Constructor.		 * 		 * @param	collection	ICollection		 * @param	implementation	Array		 * 		 * @throws	AbstractMethodCallError		 */		public function AbstractArrayIterator( collection:AbstractCollection, implementation:Array )		{			precondition(collection != null);			precondition(implementation != null);						super( collection );						if (instantiatedAs(this, AbstractArrayIterator))			{				throw new AbstractMethodCallError("Cannot " +					"instantiate Abstract class.");			}						impl = implementation;		}	}}